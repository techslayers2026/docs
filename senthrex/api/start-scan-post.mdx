---
title: Start scan (POST)
description: Start a scan by sending the domain in a form-encoded body.
openapi: "openapi/senthrex.json POST /scan"
---

# Start scan (POST)

Starts a new scan for the provided `domain`.

## Body

`application/x-www-form-urlencoded`:

- `domain` (required): the domain to scan


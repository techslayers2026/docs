---
title: Start scan (GET)
description: Start a scan by sending the domain (and optional mode) as query parameters.
openapi: "openapi/senthrex.json GET /scan"
---

# Start scan (GET)

Starts a new scan for the provided `domain` query parameter.

## Query parameters

- `domain` (required): the domain to scan
- `mode` (optional): `full` (default) or `discovery`

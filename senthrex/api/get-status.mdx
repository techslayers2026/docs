---
title: Get scan status
description: Retrieve progress and current status for a scan UUID.
openapi: "openapi/senthrex.json GET /status/{scan_uuid}"
---

# Get scan status

Returns status fields like `status`, `progress_percentage`, `current_step`, and timestamps when available.

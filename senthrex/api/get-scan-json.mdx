---
title: Get raw scan JSON
description: Retrieve the raw consolidated JSON artifact for a scan UUID.
openapi: "openapi/senthrex.json GET /json/{scan_uuid}"
---

# Get raw scan JSON

Returns the raw stored JSON artifact for the scan (`mega.json`) exactly as generated by the backend.

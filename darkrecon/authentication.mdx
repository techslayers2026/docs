---
title: Authentication
description: Authenticate to DarkRecon with an API key header.
---

# Authentication

All DarkRecon endpoints require an API key header.

## Header

- `X-API-Key: <your_api_key>`

## Example

```bash
curl -X GET "https://darkrecon.1337807.xyz/scan/YOUR_SCAN_ID" \
  -H "X-API-Key: $DARKRECON_API_KEY"
```

## Errors

- `403` indicates a missing/invalid API key.
